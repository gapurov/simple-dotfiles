# Sensible, minimal tmux configuration
set -g mouse on
set -g history-limit 10000
set -g base-index 1
setw -g pane-base-index 1

# Split helpers
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Use vim-style keys in copy mode
setw -g mode-keys vi

